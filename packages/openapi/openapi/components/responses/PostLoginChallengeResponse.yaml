type: object
description: Challenge required response
required:
  - requires
  - totp
properties:
  requires:
    type: string
    description: A code referring to why the user was not authorised
    enum:
      - challenge
  totp:
    type: boolean
    description: Whether TOTP can be used to satisfy the challenge
    example: true
