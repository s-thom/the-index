{
  "extends": ["../../.eslintrc"],
  "parserOptions": { "project": "./tsconfig.json" },
  "rules": {
    "import/no-cycle": "warn",
    // This allows repositories to have the model and the service in one file
    "max-classes-per-file": "off"
  },
  "settings": {
    "jest": {
      "version": 26
    }
  }
}
